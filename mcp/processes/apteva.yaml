schema: apteva-app/v1
name: processes
display_name: Processes
version: 0.12.2
description: Visual company procedures with independent assignments, multi-agent execution, approval gates, recurring runs, event triggers, and native one-off tasks.
author: Apteva
homepage: https://github.com/apteva/apps/tree/main/mcp/processes
icon: /ui/icon.svg
icon_style: monochrome
tags: [productivity, processes, agents, operations]
scopes: [project, global]
min_apteva_version: "0.51.3"
requires:
  permissions: [platform.events.subscribe, db.write.app, platform.apps.call, platform.instances.read, platform.instances.write, platform.threads.write]
  apps:
    - name: tasks
      optional: true
      version: ">=3.6.0"
      reason: Optional Tasks-backed execution and scheduling when selected for an assignment.
provides:
  publishes:
    - name: process.created
      description: A procedure was created.
    - name: process.updated
      description: A procedure or its synchronization status changed.
    - name: process.state_changed
      description: A procedure was activated, paused, or archived.
    - name: assignment.created
      description: An execution assignment was created.
    - name: assignment.updated
      description: Assignment configuration or schedule changed.
    - name: assignment.state_changed
      description: Assignment status changed.
    - name: run.created
      description: A run was created.
    - name: run.updated
      description: Run progress or execution details changed.
    - name: run.state_changed
      description: Run state changed; filter to_state for completion or failure.
    - name: task.created
      description: A process step or native task was created.
    - name: task.updated
      description: Task progress, executor, or details changed.
    - name: task.state_changed
      description: Task state changed.
    - name: task.approval_requested
      description: An approval became actionable.
    - name: task.approval_resolved
      description: An approval received a decision.
    - name: trigger.updated
      description: A trigger or its synchronization status changed.
    - name: trigger.event_processed
      description: An incoming trigger event was processed.
    - name: delivery.state_changed
      description: Agent dispatch entered retrying or recovered.
  http_routes:
    - prefix: /
  mcp_tools:
    - { name: list, description: Find company procedures. }
    - { name: get, description: Read a procedure and its version history. }
    - { name: create, description: Create an unassigned draft company procedure. }
    - { name: update, description: Create an immutable procedure revision while paused or draft. }
    - { name: activate, description: Activate a procedure for use by its assignments. }
    - { name: pause, description: Stop future scheduled runs without interrupting current work. }
    - { name: archive, description: Retire a procedure and stop future scheduled runs. }
    - { name: start, description: Start one execution of an active assignment. }
    - { name: runs, description: Inspect direct and Tasks-backed execution history. }
    - { name: run_get, description: "Read a run, its immutable instructions, and workflow steps before acting." }
    - { name: run_update, description: Record progress or an explicit outcome for a direct run owned by this agent. }
    - { name: assignments, description: List saved process assignments. }
    - { name: assignment_get, description: Read assignment settings. }
    - { name: assignment_create, description: "Configure a new assignment with agents, parameter values, and schedule." }
    - { name: assignment_update, description: Revise a paused assignment. }
    - { name: assignment_activate, description: Enable an assignment. }
    - { name: assignment_pause, description: Pause one assignment. }
    - { name: assignment_archive, description: Archive one assignment. }
    - { name: step_claim, description: Claim a sequential step and reuse its run worker. }
    - { name: step_get, description: Read a workflow step and its dependency outputs. }
    - { name: step_update, description: Report step output or an explicit approval decision as its assigned executor. }
    - { name: run_cancel, description: Stop future handoffs in a structured run as coordinator or operator. }
    - { name: trigger_sources, description: "Configure, preview and inspect assignment event triggers." }
    - { name: triggers, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_get, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_create, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_update, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_activate, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_pause, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_preview, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_test_run, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_events, description: "Configure, preview and inspect assignment event triggers." }
    - { name: trigger_event_retry, description: "Retry a failed event against current trigger rules with a stable key." }
    - { name: tasks, description: "Find assigned or project work across procedure tasks, standalone tasks, and added run tasks." }
    - { name: task_runs, description: "List active runs that accept additional native tasks." }
    - { name: task_create, description: "Create one-off work or add a required or optional task to a run." }
    - { name: task_get, description: "Read task instructions, revision, execution context, and history." }
    - { name: task_update, description: "Update assigned task progress or permitted settings using the current revision." }
    - { name: task_cancel, description: "Cancel a task with a reason and its current revision." }
  ui_panels:
    - slot: project.page
      label: Processes
      icon: clipboard-list
      entry: /ui/ProcessesPanel.mjs
      suggested: true
  ui_components:
    - name: process-overview
      label: Processes
      description: Running, scheduled, and recent procedure executions.
      entry: /ui/ProcessOverviewWidget.mjs
      slots: [dashboard.home]
      suggested: true
      visibility: project
      supported_sizes: [half, full]
      default_size: half
      refresh_topics: [process.created, assignment.created, delivery.state_changed, process.updated, process.state_changed, assignment.updated, assignment.state_changed, run.created, run.updated, run.state_changed, task.created, task.updated, task.state_changed, task.approval_requested, task.approval_resolved]
      settings_schema:
        type: object
        properties:
          show_active: {type: boolean, default: true}
          show_upcoming: {type: boolean, default: true}
          show_recent: {type: boolean, default: true}
          row_limit: {type: integer, title: Visible rows, default: 4, minimum: 1, maximum: 6}
      native:
        schema: apteva-native-surface/v1
        entry: /ui/surfaces/process-overview.json
  skills:
    - name: how-to-use-processes
      body_file: skills/how-to-use-processes.md
      description: Discover, maintain, and execute company procedures with direct or Tasks-backed execution.
      metadata:
        category: productivity
runtime:
  kind: source
  source:
    repo: github.com/apteva/apps
    ref: processes/v0.12.2
    entry: mcp/processes
  port: 8080
  health_check: /health
  resources: { cpu: 0.1, memory: 128, cpu_limit: 1, memory_limit: 256 }
  storage:
    - { name: data, mount_path: /data }
  env:
    APTEVA_GATEWAY_URL: { from: platform }
    APTEVA_APP_TOKEN: { from: platform }
    APTEVA_INSTALL_ID: { from: platform }
    APTEVA_PROJECT_ID: { from: platform }
    APTEVA_APP_CONFIG: { from: platform }
db:
  driver: sqlite
  path: /data/processes.db
  migrations: migrations/
config_schema: []
upgrade_policy: auto-patch

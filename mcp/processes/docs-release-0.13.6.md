# Processes v0.13.6

## Project map flow endpoints

- Render `Run starts` or the configured event topic as an explicit incoming endpoint.
- Connect incoming endpoints to root steps with dashed arrows.
- Connect terminal steps to an explicit `Run complete` endpoint, matching the detailed flow view.

# Processes v0.13.3

## Project map alignment

- Give the four-line SOP header enough height so metadata stays inside its divider.
- Align header metadata, step cards, and supplemental execution cards to the same horizontal inset.
- Tighten the boundary bottom margin and make manual “Fit all SOPs” use the same canvas padding as automatic fitting.

# Processes v0.13.1

The project map now uses the available horizontal canvas more effectively. Process
boundaries are ordered by category and packed into wider shelves, reducing the
small, tall stack shown on large screens. React Flow uses tighter fit padding so
the map content remains readable while still fitting the viewport.

Validation: 11 Bun UI/project-map tests and the production panel build/import
verification pass.

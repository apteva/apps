# Processes v0.13.7

## Balanced project-map layout

- Pack process flows into wider shelves instead of forcing every flow into a horizontal strip.
- Keep short flows horizontal and rotate flows vertically when that uses the available canvas more effectively.
- Preserve dependency edges and explicit run-start/run-complete endpoints.

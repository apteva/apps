schema: apteva-app/v1

name: email-checker
display_name: Email Checker
version: 0.3.1
description: |
  Stateless email validation. Live checks (RFC 5322 syntax, DNS MX
  lookup, optional SMTP RCPT probe) plus list-based classification
  (disposable provider, free provider, role account). No DB, no
  state — every call is a fresh resolution.

author: Apteva
homepage: https://github.com/apteva/apps/tree/main/mcp/email-checker
icon: /ui/icon.svg
icon_style: monochrome
tags: [email, validation, productivity]

scopes: [project, global]
min_apteva_version: "0.10.0"

requires:
  permissions:
    - net.egress

provides:
  http_routes:
    - prefix: /
  mcp_tools:
    - { name: email_check,       description: "Email validation — syntax + MX + classification, with optional SMTP RCPT TO probe via smtp=true. Returns {valid, reasons, mx, disposable, role, free, smtp}." }
  ui_panels:
    - slot: project.page
      label: Email Checker
      icon: mail-check
      entry: /ui/EmailCheckerPanel.mjs

runtime:
  kind: source
  source:
    repo: github.com/apteva/apps
    ref: main
    entry: mcp/email-checker
  port: 8080
  health_check: /health

upgrade_policy: auto-patch
